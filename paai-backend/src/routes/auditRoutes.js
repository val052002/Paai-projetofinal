import { Router } from 'express';

const router = Router();

// POST   /audits
// GET    /audits
// GET    /audits/:id
// POST   /audits/:id/responses
// POST   /audits/:id/finalize
// GET    /audits/:id/report
// GET    /audits/:id/report/pdf

export default router;
